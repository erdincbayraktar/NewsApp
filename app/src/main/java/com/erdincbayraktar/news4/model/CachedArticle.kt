package com.erdincbayraktar.news4.model

import androidx.room.Entity
import androidx.room.PrimaryKey

/*
@Entity(tableName = "cached_articles")
data class CachedArticle(
    val cachedAuthor: String,
    val cachedTitle: String,
    val cachedDescription: String,
    @PrimaryKey
    val cachedUrlToWebsite: String,
    val cachedUrlToImage: String,
    val cachedPublishedAt: String,
    val cachedContent: String
)

 */